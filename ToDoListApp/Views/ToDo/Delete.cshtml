@model DeleteToDoViewModel

<h1>Delete ToDo</h1>

<div class="alert alert-danger">
    Are you sure you want to delete the ToDo: <strong>@Model.Name</strong>?
</div>

<form asp-action="DeleteConfirmed" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />
    <input type="hidden" asp-for="ListId" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-action="Index" asp-route-listId="@Model.ListId" class="btn btn-secondary">Cancel</a>
</form>
